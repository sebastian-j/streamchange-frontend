import styled from 'styled-components';

export const CompatibilityInfo = styled.div`
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 100%;
  div {
    font-size: 3vw;
    text-align: center;
  }
`;
