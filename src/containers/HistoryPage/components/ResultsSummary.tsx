import styled from 'styled-components';

export const ResultsSummary = styled.span`
  color: ${(props) => props.theme.secondaryTextColor};
  font-family: 'Roboto', 'Helvetica', 'Arial', sans-serif;
  font-size: 0.85rem;
`;
