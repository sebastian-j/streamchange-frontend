/*
 * FortuneWheelRaffle Messages
 *
 * This contains all the text for the FortuneWheelRaffle component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'streamchange.components.FortuneWheelRaffle';

export default defineMessages({
  continueBtn: {
    id: `${scope}.continueBtn`,
    defaultMessage: 'Continue',
  },
});
